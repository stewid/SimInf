language: r
sudo: required

os:
  - linux
  - osx

apt_packages:
  - libgsl0-dev

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr); coveralls()'

notifications:
  email:
    on_success: change
    on_failure: change
